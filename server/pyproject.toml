[project]
name = "server"
version = "0.1.0"
description = "API for FridgePeek to run Inference for Object Detection and Classification"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.1",
    "flask-cors>=6.0.1",
    "requests>=2.32.4",
    "ultralytics>=8.3.163",
]
